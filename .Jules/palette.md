## 2026-08-09 - Added Global Keyboard Focus Indicator
**Learning:** The Astro layout in this design system uses global CSS tokens () and needed a global `:focus-visible` rule since interactive elements lacked explicit focus outlines.
**Action:** Use global CSS and design tokens like `var(--green)` in Layout.astro to enforce keyboard accessibility across all components.
## 2026-08-09 - Added Global Keyboard Focus Indicator
**Learning:** The Astro layout in this design system uses global CSS tokens and needed a global :focus-visible rule since interactive elements lacked explicit focus outlines.
**Action:** Use global CSS and design tokens in Layout.astro to enforce keyboard accessibility across all components.
