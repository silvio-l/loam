# Dart / Pub
.dart_tool/
.packages
build/
# Hinweis: pubspec.lock wird BEWUSST eingecheckt — Loam ist eine CLI/Executable
# (Dart-Konvention für Apps), nicht eine reine Library.

# Loam runtime artifacts
.loam/
baseline.json
# Default output of `loam scan --format html` — never commit a generated report.
loam-report.html

# Lokales Issue-Tracking & Doku (PROTECTED — niemals nach GitHub)
.scratch/

# Lokales Dev-Tooling (PROTECTED — niemals nach GitHub)
dev-tools/

# Lokale Claude-Code-Projektkonfig & Dev-Skills (PROTECTED — niemals nach GitHub).
# Das GANZE .claude/ bleibt lokal: Settings, lokale Skills, etwaiger Auth-/State.
/.claude/

# Secrets / Tokens / Credentials (PROTECTED — niemals nach GitHub). Zusätzlich
# HART erzwungen durch den Content-Secret-Scan in .githooks/pre-commit + pre-push.
.env
.env.*
!.env.example
*.pem
*.p12
*.key
*.token
*.secret
*credentials*.json
.netrc
id_rsa
id_ed25519

# OS / Editor
.DS_Store
*.iml
.idea/

# README/Docs-Attestierung (lokaler Push-Stempel)
.docs-attest
.readme-stamp

# Wrangler (Cloudflare) lokaler Cache
.wrangler/

# Interne, nicht-öffentliche Doku & Agenten-/Repo-Guides (PROTECTED — niemals
# nach GitHub). Bleiben lokal lesbar für dich/Agenten, sind aber untracked.
# Mechanisch zusätzlich durch die Denylist in .githooks/pre-commit abgesichert.
/CLAUDE.md
/CONTEXT.md
/docs/PRD.md
/docs/zielgruppe.md
/docs/public-docs-spec.md
/docs/readme-spec.md
/docs/adr/
