/// The main library file — declares MyLibrary and includes a part file.
library;

part 'my_library_part.dart';

/// A class declared in the library file itself.
class MyLibrary {
  /// Returns the library name.
  String get name => 'my_library';
}
