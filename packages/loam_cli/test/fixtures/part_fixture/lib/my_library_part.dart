part of 'my_library.dart';

/// A class declared in the part file — reachable via the library's element model.
class MyLibraryPart {
  /// Returns the part name.
  String get name => 'my_library_part';
}
