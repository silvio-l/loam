// Entry point — keeps the fixture a valid Dart package.
// This file uses no symbols from lib/ intentionally, so all lib/ classes
// remain unused (no external consumers) and trigger findings.
void main() {}
