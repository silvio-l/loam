analyzer:
  errors:
    unused_element: ignore
    # @internal is only valid on private-API elements; fixture exercises the
    # annotation recognition in a public lib file by design.
    invalid_internal_annotation: ignore
