name: loam
description: "Codebase intelligence and anti-AI-slop for Dart & Flutter: dead code, cycles, duplication, complexity and architecture drift, behind a baseline/ratchet CI gate."
version: 0.0.2
repository: https://github.com/silvio-l/loam
homepage: https://getloam.dev

environment:
  sdk: ^3.11.0

executables:
  loam: loam
dependencies:
  analyzer: ^13.0.0
  args: ^2.7.0
dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.1
